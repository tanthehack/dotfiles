# tanya's nix config

to use:

- make changes in [home.nix](./home.nix#L6) and the [homebrew section of configuration.nix](./configuration.nix#L57)
- update with `nix flake update`
- `sudo darwin-rebuild switch`
